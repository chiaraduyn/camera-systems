# Cameras

This page points to per-vendor camera configuration templates and notes. For full device instructions, check the `cameras/` folder in the repository and the device-specific guides in `docs/scenarios/`.

- GeoVision: see `cameras/geovision/config.example.json` and `cameras/geovision/README.md` in the repo root.

If you'd like, I can copy selected `cameras/<vendor>/README.md` files into this `docs/` site so the content is fully browsable on ReadTheDocs without jumping to raw files.

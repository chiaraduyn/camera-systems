# Scripts

This folder contains small helper scripts for testing and maintenance. They are examples — modify as needed for your environment.

Contents:
- `check_ftp_connect.sh` — quick test to see if FTP server accepts connections (uses curl or ftp commands).
- `rotate_images.sh` — (placeholder) script to rotate or cleanup old snapshots in a folder.
